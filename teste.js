// Simula carregamento e redireciona para a tela principal
setTimeout(() => {
  // Substitua pelo nome do seu arquivo principal (ex: dashboard.html)
  // window.location.href = 'dashboard.html';
  console.log("Loading completed - redirect to main app");
}, 1500);
